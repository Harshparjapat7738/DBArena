<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.DBArena</groupId>
    <artifactId>backend</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>platform-bom</artifactId>
  <packaging>pom</packaging>

  <name>DBArena :: Platform BOM</name>
  <description>
    Single place dependency versions are pinned for the whole reactor.
    Every other module imports this in its own dependencyManagement
    (scope=import) instead of declaring versions itself. See root
    CLAUDE.md "Stack - non-negotiable".
  </description>

  <dependencyManagement>
    <dependencies>

      <!-- Spring Boot / Spring Cloud - imported first so our own pins below
           can override a transitive version when we need a newer patch. -->
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- Testing -->
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>${testcontainers.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.tngtech.archunit</groupId>
        <artifactId>archunit-junit5</artifactId>
        <version>${archunit.version}</version>
      </dependency>
      <dependency>
        <groupId>net.jqwik</groupId>
        <artifactId>jqwik</artifactId>
        <version>${jqwik.version}</version>
      </dependency>

      <!-- Statement validation: JSqlParser only. Regex-based SQL filtering
           is forbidden by hard rule #4 in CLAUDE.md - do not add one. -->
      <dependency>
        <groupId>com.github.jsqlparser</groupId>
        <artifactId>jsqlparser</artifactId>
        <version>${jsqlparser.version}</version>
      </dependency>

      <!-- Events -->
      <dependency>
        <groupId>org.apache.avro</groupId>
        <artifactId>avro</artifactId>
        <version>${avro.version}</version>
      </dependency>

      <!-- Security - not managed by spring-boot-dependencies/spring-cloud-dependencies
           (see backend/pom.xml's nimbus-jose-jwt.version comment for how that was
           actually confirmed, not assumed). common-security uses this directly for
           HS256 signing/verification (M01), not through spring-security-oauth2-jose. -->
      <dependency>
        <groupId>com.nimbusds</groupId>
        <artifactId>nimbus-jose-jwt</artifactId>
        <version>${nimbus-jose-jwt.version}</version>
      </dependency>

      <!-- Engines -->
      <!-- mongodb-driver-sync, -core and bson are always released together as
           one matched triad. mongock-bom (imported below) also declares its
           own dependencyManagement entries for -core/bson - pinned to
           whatever driver version Mongock itself was tested against, which
           is NOT necessarily this one. Left unpinned here, mongock-bom's
           entries would win for -core/bson (nothing else overrides those two
           GAs) while this explicit entry still wins for mongodb-driver-sync
           itself, producing a mixed-version triad: a real, previously-latent
           bug that surfaced only once a Mongo-backed service was actually
           booted (identity-service's Postgres->Mongo migration - see
           backend/CLAUDE.md's Session Log) as
           `NoClassDefFoundError: com/mongodb/internal/TimeoutSettings` -
           a class that exists in 5.2.0's driver-core but not in the 5.0.1
           mongock-bom otherwise pins. Pinning all three explicitly, to the
           same version, keeps the triad internally consistent regardless of
           import order. -->
      <dependency>
        <groupId>org.mongodb</groupId>
        <artifactId>mongodb-driver-sync</artifactId>
        <version>${mongodb-driver.version}</version>
      </dependency>
      <dependency>
        <groupId>org.mongodb</groupId>
        <artifactId>mongodb-driver-core</artifactId>
        <version>${mongodb-driver.version}</version>
      </dependency>
      <dependency>
        <groupId>org.mongodb</groupId>
        <artifactId>bson</artifactId>
        <version>${mongodb-driver.version}</version>
      </dependency>
      <dependency>
        <groupId>org.flywaydb</groupId>
        <artifactId>flyway-core</artifactId>
        <version>${flyway.version}</version>
      </dependency>
      <dependency>
        <groupId>org.flywaydb</groupId>
        <artifactId>flyway-database-postgresql</artifactId>
        <version>${flyway.version}</version>
      </dependency>
      <dependency>
        <groupId>io.mongock</groupId>
        <artifactId>mongock-bom</artifactId>
        <version>${mongock.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- Observability -->
      <dependency>
        <groupId>io.opentelemetry</groupId>
        <artifactId>opentelemetry-bom</artifactId>
        <version>${opentelemetry.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>net.logstash.logback</groupId>
        <artifactId>logstash-logback-encoder</artifactId>
        <version>${logstash-logback-encoder.version}</version>
      </dependency>

      <!-- API documentation - every service that exposes HTTP adds this
           (root CLAUDE.md definition of done: "updated OpenAPI"). Generates
           /v3/api-docs and /swagger-ui.html from annotated controllers so
           the spec can never drift out of sync with the code by hand. -->
      <dependency>
        <groupId>org.springdoc</groupId>
        <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        <version>${springdoc-openapi.version}</version>
      </dependency>

    </dependencies>
  </dependencyManagement>
</project>
